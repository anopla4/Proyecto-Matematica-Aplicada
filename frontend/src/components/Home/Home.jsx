import React, { Component } from "react";
import { Button } from "react-bootstrap";

class Home extends Component {
  render() {
    return (
      <div>
        <Button variant="dark">Cargar datos</Button>
      </div>
    );
  }
}

export default Home;
